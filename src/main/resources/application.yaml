server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost/rest_with_spring_udemy?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
